---
milestone: v1.0
audited: 2026-08-19T00:00:00Z
status: tech_debt
scores:
  requirements: 40/40
  phases: 5/5
  integration: 40/40
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 01-live-streaming-terminal
    items: []
  - phase: 02-trading-portfolio
    items: []
  - phase: 03-visual-terminal-watchlist-control
    items:
      - "TEST-03 satisfied only in the narrower pure-function sense (established repo convention since Phase 1) — no test exercises the live fetch-based watchlist add/remove network call itself"
  - phase: 04-ai-copilot
    items: []
  - phase: 05-one-command-launch
    items:
      - "Windows scripts (start_windows.ps1/stop_windows.ps1) mirror the mac scripts' logic but have never been executed on Windows — explicitly disclosed and accepted as out of scope (D-04)"
  - phase: cross-cutting
    items: []
---

# Milestone v1.0 Audit — FinAlly AI Trading Workstation

**Audited:** 2026-08-19
**Status:** ⚡ Tech Debt (no blockers — all requirements satisfied; remaining debt is one disclosed, accepted scope gap)

## Summary

All 5 phases complete, all 40 v1 requirements satisfied, and cross-phase integration is clean with zero wiring gaps. The milestone's definition of done — a user can watch live prices stream, trade a simulated portfolio, and have an AI assistant execute trades and manage the watchlist through natural language, all in one Docker container — is met and independently re-verified by both phase-level verification and a fresh integration check in this audit.

**Correction:** the first pass of this audit read only each phase's `VERIFICATION.md` and listed every deferred human-verification item as outstanding tech debt. That was wrong — Phases 1, 2, and 3 each have a `*-UAT.md` sign-off file (not read in the first pass) showing every one of those items completed and passed: Phase 1 UAT 5/5 pass, Phase 2 UAT 4/4 pass (including the 5 judgment-tier prohibitions), Phase 3 UAT 10/10 pass. Those items are removed from tech debt below.

The `tech_debt` status (rather than `passed`) now reflects only the disclosed, accepted Windows-scripts scope limitation (Phase 5, D-04) — the sole remaining item, and one this milestone always intended to accept rather than fix (D-04). None of these block the milestone. All three previously-documented code-review warnings from Phases 3 and 5 have since been fixed and verified: Phase 3's WR-03 stale main-chart header (manually re-tested by the user, 2026-08-19); Phase 3's WR-01 watchlist-add cap/insert race (wrapped in one `BEGIN IMMEDIATE` transaction mirroring `execute_trade`'s cash-balance guard, with a new concurrency regression test, 2026-08-19); and Phase 5's WR-01 Dockerfile base-image mismatch (runtime stage moved from the floating `python:3.12-slim` tag to the same `ghcr.io/astral-sh/uv:python3.12-trixie-slim` image family as the builder stage, so both stages now share one pinned Debian release; verified by building the image and confirming `/etc/os-release` reports trixie in both stages and the container serves `/api/health`, 2026-08-19). The two cross-cutting documentation gaps have also since been closed, 2026-08-20: Nyquist validation was reconciled for all 5 phases via `/gsd-validate-phase` (Phase 1's `01-VALIDATION.md` reconstructed from scratch; Phases 2-3-4's draft `VALIDATION.md` files audited and reconciled against the live test suite — all 5 now `status: validated` / `nyquist_compliant: true`, zero functional gaps found, full backend+frontend suites green), and `04-01-SUMMARY.md`'s missing `requirements-completed` frontmatter was filled in.

## Requirements Coverage (3-Source Cross-Reference)

All 40 v1 requirements cross-referenced against VERIFICATION.md status, SUMMARY.md `requirements-completed` frontmatter, and REQUIREMENTS.md traceability table.

| Category | Count | Status |
|----------|-------|--------|
| MARKET (01-04) | 4 | ✓ satisfied |
| PORT (01-10) | 10 | ✓ satisfied |
| WATCH (01-05) | 5 | ✓ satisfied |
| CHAT (01-09) | 9 | ✓ satisfied |
| UI (01-04) | 4 | ✓ satisfied |
| INFRA (01-04) | 4 | ✓ satisfied |
| TEST (01-04) | 4 | ✓ satisfied (TEST-03 partial-by-convention, see below) |
| **Total** | **40/40** | **All satisfied, 0 unsatisfied, 0 orphaned** |

Two items required manual cross-check due to a `partial` signal from the 3-source matrix (VERIFICATION passed but SUMMARY frontmatter gap) — both resolved as functionally satisfied on inspection:

- **TEST-03**: `03-02-SUMMARY.md`/`03-04-SUMMARY.md` frontmatter and prose explicitly flag "(TEST-03, partial)" — coverage follows the project's established pure-function-only testing convention (no component-rendering tests anywhere in the repo since Phase 1), so no test exercises the live `fetch`-based watchlist add/remove network call itself. `03-VERIFICATION.md` accepts this as satisfied under the established convention. Not a blocker; noted as tech debt above.
- **CHAT-08**: `04-01-SUMMARY.md` was missing the `requirements-completed` frontmatter field entirely (a tooling/documentation gap), but `04-VERIFICATION.md` independently confirmed CHAT-08 SATISFIED with a passing test (`test_live_call_uses_mandated_call_shape`) asserting the exact LiteLLM/OpenRouter/Cerebras call shape. Functionally complete. Fixed 2026-08-20 — `04-01-SUMMARY.md` frontmatter now carries `requirements-completed: [CHAT-01, CHAT-06, CHAT-08, CHAT-09, TEST-02]`.

REQUIREMENTS.md's traceability table shows all 40 requirements checked `[x]` / Complete — the documentation-sync gaps flagged during Phase 1, 2, and 4 verification (checkboxes not flipped at the time) have since been corrected.

## Phase Verification Summary

| Phase | Status | Score | Human Verification (UAT) | Notes |
|-------|--------|-------|---------------------------|-------|
| 1. Live Streaming Terminal | passed | 2/4 direct + 2 present-but-behavior-unverified in VERIFICATION.md | ✓ 5/5 passed — `01-UAT.md` | 0 anti-patterns |
| 2. Trading & Portfolio | passed | 41/41 (5 roadmap + 36 plan-level) | ✓ 4/4 passed (incl. 5 prohibitions) — `02-UAT.md` | 0 anti-patterns |
| 3. Visual Terminal & Watchlist Control | passed | 5/5 | ✓ 10/10 passed — `03-UAT.md` | 0 unresolved warnings |
| 4. AI Copilot | passed | 12/12 | None outstanding (all gated during execution, approved) | 0 anti-patterns |
| 5. One-Command Launch | passed | 25/25 (all independently re-proven live) | None needed (all live-tested) | 0 unresolved warnings |

## Cross-Phase Integration

Spawned `gsd-integration-checker` to independently verify wiring across all 5 phases. **Result: no BLOCKER or WARNING findings.**

- **Chat/manual logic reuse verified**: `backend/app/chat/service.py:execute_actions()` calls the exact same `execute_trade()` and `add_ticker()`/`remove_ticker()` functions the manual trade bar and watchlist UI use — no divergent logic path.
- **Chat-driven UI refresh verified end-to-end**: an executed chat action triggers `onActed()` → `page.tsx` refetches portfolio/history/watchlist → PositionsTable, Heatmap, PnlChart, and Header all recompute via the same `useMemo` chain the manual trade flow uses.
- **Docker image verified as the real wired app, not a stub**: 3-stage Dockerfile builds the actual Next.js export and syncs the actual backend; all 4 phase routers are mounted in `main.py` alongside the static file mount.
- **Playwright E2E suite verified as genuine, not superficial**: all 5 specs (fresh start, watchlist add/remove, buy/sell, AI-chat trade, SSE reconnect) exercise real cross-phase code paths — e.g. the AI-chat spec only mocks the LLM call itself (`LLM_MOCK=true`) and asserts the trade lands in the real positions table via the real `execute_trade` path.
- **No orphaned exports, no missing consumers**: every declared "provides" artifact across all 5 phases is consumed by a later phase or the frontend.

## Nyquist Compliance Discovery

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 1. Live Streaming Terminal | exists (status: validated) | ✓ true | none needed |
| 2. Trading & Portfolio | exists (status: validated) | ✓ true | none needed |
| 3. Visual Terminal & Watchlist Control | exists (status: validated) | ✓ true | none needed |
| 4. AI Copilot | exists (status: validated) | ✓ true | none needed |
| 5. One-Command Launch | exists (status: validated) | ✓ true | none needed |

Updated 2026-08-20 — all 5 phases reconciled via `/gsd-validate-phase`. Phase 1 had no `VALIDATION.md` and was reconstructed from `01-VERIFICATION.md`/`01-UAT.md`. Phases 2-3-4 had `status: draft` files seeded pre-execution with unfilled `TBD` task IDs; auditing them against the live test suite found zero functional coverage gaps, but did surface and fix several stale/incorrect Automated Command references left over from the pre-execution draft (e.g. Phase 3's `PORT-09` background-writer test pointed at a nonexistent `tests/market/test_snapshot_feed.py` — the file actually lives at `tests/portfolio/test_snapshot_feed.py`; Phase 4's `CHAT-02`/`CHAT-05` commands targeted test names that don't exist). Two items in Phase 3 (`WATCH-05`) and Phase 4 (`CHAT-07`) were reclassified from a claimed-but-nonexistent automated command to accurately-described Manual-Only, backed by their respective UAT/verification sign-offs. No new test files were needed — every functionally-testable requirement already had real, passing coverage; only the documentation was stale.

## Tech Debt by Phase

**Phase 1: Live Streaming Terminal**
- None. All 5 deferred human-verification items (price flash tint/fade timing, connection-dot live recovery cycle, ten-row first paint, reduced-motion suppression, row hover/focus affordances) are confirmed `pass` in `01-UAT.md`. One initial reduced-motion regression report (G-01-3) was investigated and closed as environmental (not reproducible in Chrome Incognito or Firefox; no code defect found), resolved 2026-08-16.

**Phase 2: Trading & Portfolio**
- None. All 4 deferred human-verification items — including the 5 judgment-tier prohibitions sign-off (no shaming copy, no dark-pattern framing, no simulated money presented as real, no stale price shown as live) — are confirmed `pass` in `02-UAT.md`.

**Phase 3: Visual Terminal & Watchlist Control**
- All 10 deferred human-verification items are confirmed `pass` in `03-UAT.md` (sparkline reset, heatmap sizing/color, responsive layout, chart click isolation, etc.) — no debt here.
- WR-03 (previously an unresolved warning: removing the currently-selected watchlist ticker left the main chart header frozen on the removed ticker's last price) — manually re-tested by the user on 2026-08-19 and confirmed fixed. Removed from tech debt.
- WR-01 (previously an unresolved info-level item: watchlist add's cap check and insert weren't in one transaction) — fixed 2026-08-19 in `backend/app/watchlist/service.py:add_ticker` by wrapping the count check and insert in a `BEGIN IMMEDIATE` transaction, the same pattern `execute_trade` uses for the cash-balance race; covered by a new `TestConcurrentAddTicker` regression test in `backend/tests/watchlist/test_service.py`. Removed from tech debt.
- TEST-03 covered only in the narrower pure-function sense (established convention, not a new gap)

**Phase 4: AI Copilot**
- No outstanding tech debt. All UI-facing behavior went through blocking human-verify checkpoints during execution and was approved with recorded evidence.

**Phase 5: One-Command Launch**
- WR-01 (previously an unresolved warning: Dockerfile builder stage pinned to Debian trixie while runtime stage floated on the untagged-release `python:3.12-slim`) — fixed 2026-08-19 by rebasing the runtime stage onto `ghcr.io/astral-sh/uv:python3.12-trixie-slim`, the same image family and tag as `backend-builder`, so both stages are guaranteed to share one Debian release. Verified: image builds clean, `/etc/os-release` reports trixie in the running container, and `/api/health` returns 200 from a live container run. Removed from tech debt.
- Windows scripts never executed on Windows — explicitly disclosed, accepted scope limitation (D-04)

**Cross-cutting**
- No outstanding tech debt. Nyquist validation reconciled for all 5 phases 2026-08-20 (see table above), and `04-01-SUMMARY.md`'s missing `requirements-completed` frontmatter was filled in the same day.

### Total: 1 item across 1 phase, 0 cross-cutting

---

*Audited: 2026-08-19*
*Auditor: Claude (gsd-audit-milestone)*
